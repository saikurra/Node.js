It will automatically serach for index.html and display it for the url- http://localhost:8080


You can standardise the way to start the node app using npm start. But for this to work we need to have scripts in package.json as "scripts: "node <js_file>"